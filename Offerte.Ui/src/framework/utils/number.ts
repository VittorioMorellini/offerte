export { }
// import Decimal from 'decimal.js';

// export function format(value: Decimal, decimalNumbers: number = 2, decimalSeparator: string = ',') {
    
//     return value.toDecimalPlaces(decimalNumbers, Decimal.ROUND_HALF_UP).toFixed(decimalNumbers).replace('.', decimalSeparator);
// }